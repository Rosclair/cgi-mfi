<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect everything to the public/ directory (Symfony front controller)
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>
